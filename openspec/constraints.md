# OpenSpec Constraint Set

---

## 变更 1: 动态版本加载功能

### 用户需求
> downloads不是很能反映sh的链接，因为要开源，要是能的动态加载最新

### 实施状态
✅ **已实施** - setup.sh 已更新为动态版本加载模式

## 用户需求
> downloads不是很能反映sh的链接，因为要开源，要是能的动态加载最新

## 实施状态
✅ **已实施** - setup.sh 已更新为动态版本加载模式

## 硬约束 (Hard Constraints)

### 1. 版本发现机制 ✅
- ✅ 使用 GitHub Releases API `/releases/latest` 端点
- ✅ API 端点无认证访问（公开端点）
- ✅ 仓库地址固定：
  - v2rayA: `https://api.github.com/repos/v2rayA/v2rayA/releases/latest`
  - Xray-core: `https://api.github.com/repos/XTLS/Xray-core/releases/latest`

### 2. 资源命名模式 ✅
- ✅ v2rayA debian x64: `installer_debian_x64_{VERSION}.deb`
- ✅ Xray-core linux x64: `Xray-linux-64.zip`
- ✅ 版本号格式：`v{major}.{minor}.{patch}`（如 v2.2.7.5, v26.2.6）

### 3. 下载 URL 构造 ✅
- ✅ v2rayA: `https://github.com/v2rayA/v2rayA/releases/download/{version}/installer_debian_x64_{version}.deb`
- ✅ Xray-core: `https://github.com/XTLS/Xray-core/releases/download/{version}/Xray-linux-64.zip`

### 4. 失败处理 ✅
- ✅ API 调用失败时报错并退出
- ✅ 资源下载失败时报错并退出
- ✅ 版本号解析失败时报错并退出
- ✅ 不静默失败或使用默认值

### 5. 兼容性约束 ✅
- ✅ 保持 `install_v2raya_simple.sh` 的调用接口不变
- ✅ 文件结构检查逻辑适配新格式
- ✅ 处理 `.deb` 和 `.zip` 两种不同的包格式

## 软约束 (Soft Constraints)

### 1. 用户体验 ✅
- ✅ 显示正在获取的版本号
- ✅ 显示下载进度（wget --show-progress）
- ✅ 提供清晰的错误信息

### 2. 代码可读性 ✅
- ✅ 使用清晰的变量名
- ✅ 添加必要的注释
- ✅ 遵循现有的脚本风格

### 3. 架构扩展性 ✅
- ✅ 支持检测系统架构（amd64, arm64, armv7, loongarch64, mips32/64, riscv64 等）
- ✅ 根据架构选择对应的包

## 依赖关系

### 外部工具依赖 ✅
- ✅ `curl` 或 `wget`：用于下载文件和 API 调用
- ✅ `jq`：用于解析 JSON（GitHub API 响应）
- ✅ `dpkg`：用于安装 .deb 包
- ✅ `unzip`：用于解压 .zip 文件（Xray-core）

### 内部依赖 ✅
- ✅ `install_v2raya_simple.sh`：动态生成的实际安装脚本

## 实施总结

### 主要变更文件
- `setup.sh` - 完全重写，新增以下功能：
  - `detect_arch()` - 自动检测系统架构
  - `check_dependencies()` - 检查必要工具
  - `get_latest_version()` - 从 GitHub API 获取最新版本
  - `download_file()` - 统一下载函数
  - 动态生成 `install_v2raya_simple.sh`

### 执行流程
```
1. 检测系统架构 → 2. 检查依赖工具 → 3. 获取最新版本
4. 下载 v2rayA .deb → 5. 下载 Xray .zip → 6. 解压 Xray
7. 生成安装脚本 → 8. 执行安装
```

## 风险缓解措施

| 风险 | 缓解措施 |
|------|----------|
| GitHub API 速率限制 | 无认证端点足够，已添加错误处理 |
| 网络连接问题 | wget 显示进度并提供清晰错误信息 |
| 版本不兼容 | GitHub 发布版本经过测试，风险较低 |
| jq 依赖缺失 | 检查 jq 是否安装，提供安装提示 |

## 成功判据 ✅

1. ✅ 脚本能够成功获取最新的 v2rayA 和 Xray-core 版本号
2. ✅ 能够正确下载对应版本的安装包（.deb 和 .zip 格式）
3. ✅ 安装过程能够正常完成
4. ✅ 安装的软件版本与 GitHub 最新版本一致
5. ✅ 错误情况下能够提供清晰的错误信息并退出

## 后续扩展建议

1. **多架构测试** - 在 arm64 等非 amd64 平台测试
2. **版本锁定** - 添加 `--version` 参数允许指定特定版本
3. **离线模式** - 支持从本地缓存安装
4. **镜像源** - 添加国内镜像源支持以加快下载速度
5. **签名验证** - 添加 GPG 签名验证确保下载完整性

---

## 变更 2: 一条 curl 命令完成全自动安装

### 用户需求
> 当前 setup.sh 能否用一条curl命令行完成全自动安装和启动和自启动

### 实施状态
🔄 **代码实施完成** - 约束分析、提案和代码修改已完成，待部署测试

### 变更详情
详细约束和实施计划请参考: [openspec/changes/one-curl-install/constraints.md](./changes/one-curl-install/constraints.md)

### 目标命令
```bash
curl -fsSL <public-url> | sudo bash
```

### 关键变更
| 项目 | 变更 |
|------|------|
| `check_dependencies()` | 修改为自动安装依赖（jq, unzip, curl/wget） |
| 部署 | 将脚本托管到 GitHub raw URL |
| 用户体验 | 一条命令完成从下载到服务运行 |

---

## 变更 3: 创建 README.md

### 用户需求
> 缺少README.md 先创建，尽量少使用emoji

### 实施状态
✅ **已实施** - README.md 已创建，符合无 emoji 约束

### 变更详情
详细约束和实施计划请参考: [openspec/changes/readme-creation/constraints.md](./changes/readme-creation/constraints.md)

### 关键约束
| 约束 | 说明 |
|------|------|
| 不使用 emoji | 用户明确要求 |
| 反映当前代码状态 | 基于实际的 setup.sh 功能 |
| 包含一条命令安装 | 提及 curl 管道安装方式 |
| 目标系统明确 | Ubuntu 22.04 (Jammy Jellyfish) |
